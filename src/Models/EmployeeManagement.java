package Models;

import java.util.ArrayList;

public class EmployeeManagement {
    private ArrayList<Candidate> candidates;

    public void addCan(Candidate newCan) {
        candidates.add(newCan);
    }
    //Create
    //Search
}
